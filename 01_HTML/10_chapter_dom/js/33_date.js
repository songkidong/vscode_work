function getDate() {
    // 선택함수 : 달력
    let dateVal = document.querySelector("#date");
    alert(dateVal.value);
}
function getTime() {
    // 선택함수 : 시간
    let timeVal = document.querySelector("#time");
    alert(timeVal.value);
}